spring:
  # datasource:
  #    url: jdbc:h2:mem:testdb -- in memory DB  테스트용 . 주석처리해도 스프링 부트가 알아서 in memory로 돌려줌
  #    username: sa
  #    password:
  #    driver-class-name: org.h2.Driver


#  jpa:                       -- 마찬가지로 jpa 설정도 spring boot가 기본적으로 해줌. roll back해서 지워줌.
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging.level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
